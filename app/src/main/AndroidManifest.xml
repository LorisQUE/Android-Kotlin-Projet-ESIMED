<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.esimed.mabanque">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".OperationRapprochementActivity"
            android:label="@string/title_activity_operation_rapprochement"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".OperationEditActivity"
            android:label="@string/title_activity_operation_edit"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".OperationAddActivity"
            android:label="@string/title_activity_operation_add"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".OperationActivity"
            android:label="@string/title_activity_operation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AccueilActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>